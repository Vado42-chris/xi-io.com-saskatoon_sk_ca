# Snapshots

Snapshots are preserved copies of structured public datasets when rights allow reuse.

Before adding a snapshot, confirm:

- Source is listed in `source.registry.json`
- Rights status allows storage
- Access timestamp is captured
- Snapshot method is documented
- Hash is captured where practical
- Sensitive fields are removed or generalized if needed

If rights are unclear, store metadata and a research note instead of the snapshot.
